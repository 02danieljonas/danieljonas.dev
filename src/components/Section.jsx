// import React from 'react'
// import { useRef } from 'react'
// //a template for reacting the sections
// //        <section name="About" key="1" ref={refList[1]}>
// //every single time its called it pushes to the list
// //

// const Section = ({name, listOrder, children, refList}) => {
//     refList[listOrder] = (useRef(null))

//     return (
//         <section name={name} key={listOrder} ref={refList[listOrder]}>
//             {children}
//         </section>
//         // console.log(   JSON.stringify(name, listOrder, children))
//     // <section name={name} key={listOrder} ref={refList[listOrder]}>
//     //     {info}
//     // </section>,
//     )
// }

// export default Section